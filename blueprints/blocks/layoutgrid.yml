name: Layout Grid
icon: grid
fields:
  layoutgridblocksforfieldsets:
    type: blocks
    # Remember to set the same blocks (except layoutgrid)
    # in Kirby page blueprint
    fieldsets:
      - heading
      - text
      - image
    #  - layoutgrid
      - quote
      - video
      - list
      - gallery
  layoutgridstructure:
    type: structure
    fields:
      layoutgridblocks:
        type: blocks
      grid_row_start:
        type: text
      grid_column_start:
        type: text
      grid_row_span:
        type: text
      grid_column_span:
        type: text
